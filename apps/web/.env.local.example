# ─── JWT (aceite LGPD / sessão httpOnly) ──────────────────────────────────────
# Segredo HS256 compartilhado com o backend. Sem fallback no código: ausente = erro no boot.
# Gere com: openssl rand -base64 32
JWT_SECRET=change-me-use-openssl-rand-base64-32
